##
# Expo public env vars (available in JS as process.env.EXPO_PUBLIC_*)
#
# Copy this file to `mobile/.env` (or set env vars in your shell) and restart Expo.
#
# Physical device (Expo Go): set this to the LAN IP of your PC shown by Expo as:
#   Metro waiting on exp://<IP>:8081
##

# Use Firebase emulators in development.
EXPO_PUBLIC_USE_EMULATORS=1

# LAN IP of the machine running Firebase emulators (required on physical devices).
EXPO_PUBLIC_EMULATOR_HOST=192.168.X.Y

# Physical device (Expo Go on iOS/Android):
# Set this to the LAN IP of your PC (the same IP shown by Expo as exp://<IP>:8081)
EXPO_PUBLIC_EMULATOR_HOST=192.168.200.104

# Optional: force on/off. If not set, defaults to enabled in dev (__DEV__), disabled in prod.
EXPO_PUBLIC_USE_EMULATORS=1


